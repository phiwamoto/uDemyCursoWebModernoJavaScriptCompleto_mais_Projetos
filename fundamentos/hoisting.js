console.log('a =', a)
var a = 2
console.log('a =', a)


console.log('b =', b) // Comentar essa linha para processar, no caso do Let da o erro
let b = 2
console.log('b =', b)

// Hoisting O Hoisting permite que você execute funções antes das suas declarações. 
// Na prática, inicialmente as declarações de funções são colocadas na memória durante a fase de compilação e, mesmo assim, permanecem no mesmo lugar que estão digitadas